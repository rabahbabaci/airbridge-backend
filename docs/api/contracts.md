# API Contracts (Draft v0)

## POST /v1/trips
Create/validate a trip context.

## POST /v1/recommendations
Compute leave-home recommendation.

## POST /v1/recommendations/recompute
Recompute recommendation for existing trip.
